apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: checkouts.flotilla.work
spec:
  group: flotilla.work
  scope: Namespaced
  names:
    plural: checkouts
    singular: checkout
    kind: Checkout
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Env
          type: string
          jsonPath: .spec.env_ref
        - name: Phase
          type: string
          jsonPath: .status.phase
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
