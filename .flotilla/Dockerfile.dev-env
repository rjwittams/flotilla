FROM rust:1-bookworm

# System tools for development
RUN apt-get update && apt-get install -y \
    git \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Install shpool (terminal pool)
RUN cargo install shpool --locked

# Working directory
WORKDIR /workspace

# Keep container running
CMD ["sleep", "infinity"]
