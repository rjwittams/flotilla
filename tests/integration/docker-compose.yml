services:
  node-a:
    build:
      context: ../..
      dockerfile: tests/integration/docker/base/Dockerfile
      target: full
    hostname: node-a
    volumes:
      - shared-keys:/shared-keys

  node-b:
    build:
      context: ../..
      dockerfile: tests/integration/docker/base/Dockerfile
      target: full
    hostname: node-b
    volumes:
      - shared-keys:/shared-keys

volumes:
  shared-keys:
